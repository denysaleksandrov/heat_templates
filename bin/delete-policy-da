#!/bin/bash

if [[ $# -ne 3 ]]; then
	echo "Usage: delete-policy tenant-name policy-name VN"
        exit 1
fi

args=("$@")
TENANT=${args[0]}
PNAME=${args[1]}
VN=${args[2]}

heat --os-tenant-name $TENANT stack-delete $PNAME-attach-to-$VN
